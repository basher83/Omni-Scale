# Proxmox Provider Environment Configuration
# Copy to .env and fill in actual values

# Tailscale auth key (generate from Tailscale admin console)
# Use a reusable, ephemeral key tagged for the provider
TS_AUTHKEY=tskey-auth-XXXXXXXXXXXX-XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Omni service account key (generate in Omni UI: Settings → Service Accounts)
# Base64-encoded PGP key for provider authentication
OMNI_SERVICE_ACCOUNT_KEY=eyJ...base64-encoded-key...

# Provider ID (must match what's registered in Omni)
PROVIDER_ID=matrix-cluster

# Omni API endpoint
OMNI_API_ENDPOINT=https://omni.spaceships.work/
