# MachineClass for worker on Foxtrot
# Storage: vm_ssd (CEPH RBD, distributed storage)
# Pinned to foxtrot node
# Note: Foxtrot also hosts provider LXC (minimal footprint)

metadata:
  namespace: default
  type: MachineClasses.omni.sidero.dev
  id: matrix-worker-foxtrot
spec:
  matchlabels: []
  autoprovision:
    providerid: matrix-cluster
    kernelargs: []
    metavalues: []
    providerdata: |
      cores: 8
      sockets: 1
      memory: 16384
      disk_size: 100
      network_bridge: vmbr0
      storage_selector: name == "vm_ssd"
      node: foxtrot
      cpu_type: host
      machine_type: q35
      balloon: false
      disk_ssd: true
      disk_discard: true
      disk_cache: none
      disk_iothread: true
      disk_aio: io_uring
