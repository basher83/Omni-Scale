---
omni_endpoint: https://omni.<your-tailnet>.ts.net
proxmox_endpoint: https://<proxmox-node>.<your-tailnet>.ts.net:8006
provider_status: unknown
last_verified: null
active_clusters: []
storage_selector: 'storage.filter(s, s.type == "rbd" && s.storage == "<pool-name>")[0].storage'
default_cpu: 4
default_memory: 8192
default_disk: 40
---

# Omni-Scale Deployment State

Copy this file to `.claude/omni-scale.local.md` and fill in your values.

## Current Status

- **Provider:** Not configured
- **Machine Classes:** None
- **Clusters:** None

## Next Steps

1. Generate infrastructure provider key in Omni UI (Settings → Infrastructure Providers)
2. Run `/provider-setup` to configure the provider
3. Run `/provider-status` to verify connectivity
4. Create machine classes with `/machineclass-create`
5. Check cluster status with `/cluster-status`

## Configuration Notes

Replace placeholders:

- `<your-tailnet>` - Your Tailscale tailnet name
- `<proxmox-node>` - Your Proxmox node hostname
- `<pool-name>` - Your storage pool name (e.g., vm_ssd, local-lvm)
